<article class="project" id="<%= dom_id(project) %>">
  <h3><%= project.title %></h3>
  <p><%= project.description %></p>
  <div class="nav">
    <%= link_to 'Show', project, remote: true, class: 'remote-show' %> |
    <%= link_to 'Edit', edit_project_path(project), remote: true, class: 'remote-edit' %> |
    <%#= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }, remote: true %>
    <%#= link_to 'Destroy', project, class: 'remote-delete' %>
    <%#= link_to 'Destroy', project, method: :delete, remote: true, class: 'remote-delete' %>
    <%= link_to 'Delete', project, method: :delete, data: { confirm: 'Are you sure?' }, remote: true, class: 'remote-delete' %>
  </div>
</article>
