require "test_helper"

feature "TBD" do
  # describe ContactsController do
  #   it "must be a real test" do
  #     flunk "Need real tests"
  #   end
  scenario "do we need this?" do
    skip("will figure out if we need this")
  end
end
